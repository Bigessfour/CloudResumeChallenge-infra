# Dev / test dependencies for the visitor counter Lambda (not packaged into the deployment zip).
# boto3 is provided by the Lambda runtime in production; we pin it here for tests + type parity.
boto3>=1.34,<2
moto[dynamodb]>=5.0,<6
pytest>=8.0,<9
